# Heap expansion status

## End goal

Support a general, grow-only, expandable major heap for the verified OCaml generational GC while preserving the same end-to-end correctness story as the original fixed-size major heap.

Concretely, the major heap should become a non-moving collection of active chunks in one virtual address namespace. Existing object addresses must remain stable. Expansion should acquire a fresh disjoint chunk, initialize it as blue free-list memory, link it into the global major free list, and then proceed through the verified allocation/collection path. The collector should retain the existing guarantees: live objects survive, roots are rewritten correctly, major and minor invariants are preserved, pointer/graph edges are preserved through forwarding and update, and no new untracked admits or runtime assumptions are introduced.

The intended runtime policy is preflight expansion, not mid-promotion expansion: before a Cheney minor collection, compute a conservative promotion demand, ensure the major free-list head has enough contiguous capacity, run/expand if needed, and only then start promotion. If ordinary major allocation fails, the runtime should run major GC, retry, acquire/register a fresh chunk if still short, call the verified expansion/linking path, and retry allocation.

## Current progress

The proof development has moved from planning into a substantial verified chunked-major model and chunked generational collection shell.

- **Pure chunked heap model:** `GC.Spec.MajorHeap` models active chunks, disjoint chunk ranges, chunk-local/global object enumeration, chunked reads/writes, old-read framing, add-chunk framing, and single-chunk compatibility with the original dense heap.
- **Range-owned implementation model:** `GC.Impl.MajorHeap` owns active chunk ranges with `pts_to_range`, has verified chunk-local read/write helpers, indexed chunk ownership, and single-chunk adapters to/from the legacy dense heap predicate.
- **Chunked allocator/expansion:** `GC.Spec.MajorAllocator` has fresh-chunk initialization, free-list linking, chunked first-fit allocation, expand-on-OOM specs, capacity/head-capacity preflight specs, preservation of free-list validity/termination/fit/above-zero, and verified Pulse wrappers for owned expansion/allocation.
- **Generational heap invariants:** chunked analogues now exist for allocator shape, no-blue minor-to-major fields, no-infix major-to-minor fields, no-black/no-scan/no-pointer-to-blue, chain-blue side invariants, and collection-shape preservation through expansion/preflight.
- **Chunked Cheney promotion and update:** `GC.Gen.ChunkedPromote`, `GC.Gen.ChunkedCheney`, and `GC.Gen.ChunkedUpdate` now provide chunked promotion, forwarding loops, full collection shell, major-pointer update, single-chunk compatibility, no-OOM/preflight bridges, forwarding-target shape, allocator-shape preservation, old object/header/field preservation, and exact old-field update effects.
- **Client-facing chunked correctness bundle:** `GC.Gen.CheneyCorrectness.chunked_cheney_gc_correct_after_preflight` packages optional head expansion, chunked promotion/update/root rewrite/minor reset, reachable-minor forwarding, allocator-shape preservation, forwarding-target validity, old-major survival, and old-header/field preservation.
- **Graph preservation surface:** `GC.Gen.CombinedGraph` has chunked vertex characterization, chunked classification, edge constructors/eliminators, old-major field edge introduction, expansion-safe old-view graph/reachability preservation, chunked minor-field edge introduction, and old-major post-collection edge corollaries for unchanged and forwarded fields.
- **Latest proof step:** added a bundled forward graph-morphism surface and continued discharging readiness. `GC.Gen.CheneyCorrectness` maps ready source vertices and edges into the post-collection chunked major graph via `chunked_graph_maps_to_major_graph_prop`, and exposes the above-minor-target plus non-blue-source readiness reductions. The newest layers live in split graph/readiness modules: major-target branches can derive the target witness from `CG.mem_ce` plus a heap-wide `chunked_major_objects_above_minor` predicate; that predicate is connected to chunk bases, the single-chunk compatibility heap, `expand_major_heap`, and `ensure_major_head_capacity_spec`; a reachable-target readiness variant derives `MinorV -> MinorV` target positivity from the edge itself while replacing the `MajorV -> MinorV` forwarding-map premise with reachable positive minor-target facts; a vertex-membership readiness variant recovers active major vertices from `CG.mem_cv`, leaving clients to supply only reachable positive minor vertices; selected graph predicates package graph membership plus selected vertex/edge readiness into `chunked_selected_graph_maps_to_major_graph_prop`; live-selected edge readiness derives minor-source scannability from the graph edge plus chunked collection shape, provided active major objects are known to be legacy heap-pointer field values; `chunked_live_selected_graph_vertex` / `chunked_live_selected_graph_edge` package reachable positive minor vertices and active non-blue major source vertices into a client-facing live-subgraph mapping surface; the simple chunk-base policy (`chunked_major_chunks_above_zero_addr`) now derives both above-minor active objects and active-major pointer-field readiness, with a wrapper that maps the live-selected graph using only chunk-base assumptions plus the usual preflight freshness obligations; and `GC.Gen.ChunkedReachabilityBridge` now proves both halves needed to connect combined reachability to live selection. Reachable `MajorV` vertices are active non-blue chunked-major objects under chunked collection shape, root-valid-nonblue, and active-major pointer-field assumptions. Combined-reachable `MinorV` vertices are in `minor_reachable roots` when remembered major-to-minor edges are covered. `GC.Gen.CheneyGraphReadiness` packages this as `chunked_reachable_live_graph_vertex` / `chunked_reachable_live_graph_edge` and proves the reachable-positive live subgraph maps into the post-collection chunked major graph from chunk-base, root-valid, and remembered-edge coverage assumptions. `GC.Gen.ChunkedRemembered` now provides the first chunked remembered-root scan over `MH.major_objects`, proves completeness for non-field-0 active non-blue scannable major fields, derives `chunked_remembered_minor_edges_in_roots` from the smaller client obligation that all scan-produced minor roots are included in the Cheney roots, and now exposes `chunked_minor_collection_roots` as the append-based root construction that satisfies that inclusion automatically. `GC.Gen.CheneyGraphReadiness` has a scanned-roots reachable-live graph theorem that consumes base roots and the constructed root sequence rather than a raw root-inclusion premise. It also now defines the forwarding image of the reachable-live source graph, proves a `CG.reachable_subgraph_isomorphism` to that image from exactly one explicit missing fact, and splits that injectivity fact into two branch-specific obligations: forwarded reachable minor images are injective, and forwarded reachable minor images are disjoint from reachable old-major vertices. This packages the surjectivity and edge-reflection clauses of the fixed-heap-style isomorphism while isolating the remaining allocation/freshness proof. `GC.SPOT.HeapExpansion` audits the one-vertex, all-vertices, one-edge, all-edges, bundled graph-morphism, above-minor-target readiness, non-blue-source readiness, edge-derived-target readiness, reachable-target readiness, vertex-membership readiness, selected-edge/selected-graph readiness, live-selected scannability, live-selected graph mapping, active-major pointer-field construction/preservation, chunk-base-to-above-minor/pointer-field construction, chunk-base live-selected graph surface, chunked reachable-major non-blue bridge, chunked combined-minor reachability bridge, reachable major/minor live-selected wrappers, reachable-live graph mapping theorem, chunked remembered scan completeness, scan-to-remembered-edge bridge, scan-based reachable-live graph theorem, scanned-root construction theorem, reachable-live image-isomorphism wrapper, and injectivity splitter.

Recent validation before this status update included targeted F* checks for `GC.Gen.CombinedGraph`, `GC.Gen.CheneyCorrectness`, `GC.Gen.ChunkedReachabilityBridge`, `GC.Gen.ChunkedRemembered`, `GC.Gen.CheneyGraphReadiness`, and `GC.SPOT.HeapExpansion`; refreshed query-stat profiling showed `GC.Gen.ChunkedRemembered` verifying at local rlimit 1 for all scan-completeness proofs, with the largest new query using about 0.53 rlimit, and the scan-based wrapper in `GC.Gen.CheneyGraphReadiness` adding only small rlimit-1 queries. Earlier graph-morphism wrappers verified in the targeted `GC.Gen.CheneyCorrectness` profile in about 1.5 seconds overall, and the chunked vertex lemmas verified in `GC.Gen.CombinedGraph` in about 1.1 seconds overall. Full SPOT plus broad `make generational` verification passed with zero `Warning 349`.

## Update: 2026-06-08

The minor-image injectivity branch is now durable proof progress in a split module. `GC.Gen.ChunkedCheneyInjectivity` proves `chunked_fwd_normal_injective` for ordinary non-infix minor sources through full chunked Cheney promotion. The proof uses a compact "forwarded normal targets are active non-blue major objects" invariant: fresh normal forwarding targets are allocated from the current blue free-list head, while older normal forwarding targets are known non-blue and therefore cannot be on the blue free-list chain.

`GC.Gen.CheneyGraphReadiness` now wires that split theorem through optional minor-promotion head preflight and the chunked collection shell, proving `chunked_reachable_live_minor_images_injective_prop` from the same chunk-base and remembered-scan assumptions used by the reachable-live graph mapping theorem. `GC.SPOT.HeapExpansion` audits the new public wrapper.

Profiling confirmed the split proof is cheap: `GC.Gen.ChunkedCheneyInjectivity` verifies in about 1.7s with `--query_stats --z3refresh --split_queries always`, and the new graph-readiness wrapper's split queries use very small rlimits. The whole `GC.Gen.CheneyGraphReadiness` profile still takes longer because it replays the existing large readiness module under `--z3refresh`, but it completes successfully.

## Update: 2026-06-12

The minor-image/old-major disjointness branch is now proved in a second split module. `GC.Gen.ChunkedCheneyDisjointness` proves forwarded ordinary non-infix minor images remain disjoint from a fixed old major object through full chunked Cheney promotion. The proof uses free-list `major_fl_chain_avoids` rather than old-header size facts, avoiding a false dependency on reachable old major objects having positive `wosize`.

`GC.Gen.CheneyGraphReadiness` now derives that chain-avoidance premise from `chunked_chain_objects_blue` for reachable non-blue old-major vertices after optional preflight expansion, proves `chunked_reachable_live_minor_images_disjoint_from_major_prop`, composes it with the existing minor-image injectivity branch into full `chunked_reachable_live_graph_injective_prop`, and discharges the reachable-live image-isomorphism wrapper from chunk-base plus scanned-root assumptions. `GC.SPOT.HeapExpansion` audits the branch-2 wrapper and the composed injectivity/isomorphism surfaces.

Profiling kept the new split proof small: the largest `GC.Gen.ChunkedCheneyDisjointness` query used about 1.05 rlimit and 340ms under `--z3refresh --split_queries always`; the new graph-readiness branch peaked under rlimit 5. Targeted changed-module checks, `make mark-and-sweep`, and full `cd spot && make verify` passed. A full `make generational` run was started but stopped after spending a long time in pre-existing `generational/impl/GC.Gen.Impl.Cheney.fst`; the changed readiness/disjointness targets and generational orphan check passed.

The reachable-live forwarding image is now also proved to be a concrete subgraph of the post-collection chunked major graph. `GC.Gen.CheneyGraphReadiness.chunked_reachable_live_graph_image_subgraph_of_post_major_graph_from_maps` converts the already-proved reachable-live vertex/edge forward-image mapping property into image-vertex and image-edge post-graph membership, and the chunk-base/scanned-root wrapper composes it with optional preflight expansion and full chunked collection. `GC.SPOT.HeapExpansion` audits both the generic subgraph theorem and the chunk-base wrapper. The split readiness module still verifies in about 1.5s overall under `--z3refresh --split_queries always`.

The reachable-live image is now connected to post-collection reachability. `GC.Gen.CombinedGraph` exposes small SMT-facing source-shape helpers (`combined_vertex_exhaustive` and `chunked_edge_source_vertex`). `GC.Gen.CheneyGraphReadiness` proves root images appear in the classified rewritten roots, then uses source-graph induction plus the image-subgraph theorem to show every reachable-live image vertex is reachable in the post-collection chunked major graph. `GC.SPOT.HeapExpansion` audits the root-image theorem, the generic roots+subgraph reachability theorem, and the chunk-base/scanned-root wrapper. Targeted profiling kept `GC.Gen.CombinedGraph` around 1.1s and `GC.Gen.CheneyGraphReadiness` around 1.6s overall under `--z3refresh --split_queries always`; full SPOT and generational orphan checks passed. A broad `make generational` run was stopped after spending over 10 minutes in dependency generation with no additional proof output, so the current validation uses targeted changed-module checks plus closure checks.

The reachable-live graph facts are now packaged into a single post-reachable image theorem. `GC.Gen.CheneyGraphReadiness.chunked_reachable_live_graph_post_reachable_image_isomorphism_prop` states a `CG.reachable_subgraph_isomorphism` from the reachable-live source graph to the forwarding image, where destination vertices are both image vertices and post-reachable from rewritten roots, and destination edges are both image edges and concrete post-collection major edges. The chunk-base/scanned-root wrapper composes image injectivity/isomorphism, post-subgraph inclusion, and post-reachability into one client-facing theorem, audited by `GC.SPOT.HeapExpansion`. A split-query profile showed the new final theorem's individual queries stay small (roughly 80-90ms, used rlimit up to about 0.04), though an uncached whole-file `--z3refresh` pass over the large readiness module took about 4 minutes.

The final graph theorem now also has an append-based scanned-root wrapper: callers can use `CRem.chunked_minor_collection_roots minor major base_roots` and get the post-reachable image isomorphism without manually proving `chunked_minor_roots_in_roots`. This discharges one of the remaining API-threading obligations for clients that follow the verified remembered-scan construction.

The scanned-root wrapper has been strengthened one step further: `GC.Gen.ChunkedRemembered` proves every root produced by the chunked remembered scan is a minor pointer, so appending those roots preserves `chunked_roots_valid_nonblue` from the original base roots. `GC.Gen.CheneyGraphReadiness` now exposes a final post-reachable image-isomorphism wrapper that requires only `chunked_roots_valid_nonblue base_roots major`, plus the constructed `chunked_minor_collection_roots`, and `GC.SPOT.HeapExpansion` audits the reduced-premise surface. Targeted refreshed profiles kept `GC.Gen.ChunkedRemembered` around 1.1s and `GC.Gen.CheneyGraphReadiness` around 1.6s.

The fixed-heap side conditions now have single-chunk compatibility bridges into the chunked reachability surface. `GC.Gen.ChunkedReachabilityBridge.chunked_roots_valid_nonblue_single_chunk_compat` turns dense `roots_valid_nonblue` into chunked root validity, and `chunked_major_field_zero_no_minor_single_chunk_compat` turns the existing dense `major_field_zero_no_minor` assumption into the chunked predicate over `MH.single_chunk_major_heap`. `GC.SPOT.HeapExpansion` audits both wrappers. This does not eliminate the true multi-chunk field-0 policy obligation, but it prevents the chunked graph API from regressing fixed-heap clients.

The true multi-chunk field-0 policy is now stable across the Cheney preflight expansion step. `GC.Gen.ChunkedReachabilityBridge.chunked_major_field_zero_no_minor_preserved_by_expansion` proves raw `expand_major_heap` preserves the predicate when old major fields are expansion-safe, and `chunked_major_field_zero_no_minor_ensure_head_capacity` lifts it through `ensure_major_head_capacity_spec`. The proof avoids scanning/wosize assumptions for field 0 by proving old object addresses are outside the fresh chunk, and the SPOT module audits both contracts. Refreshed profiling kept the bridge module around 1.2s.

The highest-level post-reachable image API now packages those policy facts for runtime-facing callers. `GC.Gen.CheneyGraphReadiness.chunked_cheney_gc_correct_after_preflight_policy_and_post_reachable_image_from_base_roots` combines the final base-root/scanned-root image theorem with preflight preservation of chunk-base readiness, active-major pointer-field readiness, and the field-0 no-minor invariant on `ensure_major_head_capacity_spec`'s `capacity_major_out`. `GC.SPOT.HeapExpansion` audits the wrapper, and the split-module profile remains around 1.6s.

Fixed-heap callers now have a direct dense-policy bridge to that highest-level API. `chunked_cheney_gc_correct_after_preflight_policy_and_post_reachable_image_single_chunk_from_dense_roots` starts from the existing dense `roots_valid_nonblue` and `major_field_zero_no_minor` predicates over a `heap`, uses the single-chunk compatibility lemmas internally, and returns the same post-reachable image/preserved-policy result for `MH.single_chunk_major_heap major`. This keeps the compatibility path aligned while the multi-chunk runtime boundary is still being designed.

Root validity now has the matching preflight preservation bridge. `GC.Gen.ChunkedReachabilityBridge.chunked_roots_valid_nonblue_preserved_by_expansion` and `_ensure_head_capacity` preserve `chunked_roots_valid_nonblue` across fresh expansion when the pre-existing root sequence is disjoint from the fresh chunk. This isolates the runtime-side obligation that fresh chunk addresses are not already present in program roots.

The highest-level policy surface now has a full-policy variant. `GC.Gen.CheneyGraphReadiness.chunked_cheney_gc_correct_after_preflight_full_policy_and_post_reachable_image_from_base_roots` adds the root-fresh-disjoint precondition on the expansion branch and returns base-root validity on the ensured heap alongside chunk-base readiness, pointer-field readiness, field-0 preservation, and the post-reachable image-isomorphism result.

Constructed collection roots now preserve validity through preflight as well. `GC.Gen.ChunkedReachabilityBridge.chunked_roots_valid_nonblue_append_minor_pointers` factors the generic fact that appending minor-only roots cannot create invalid major roots, and `GC.Gen.ChunkedRemembered.chunked_roots_valid_nonblue_collection_roots_ensure_head_capacity` applies it to `chunked_minor_collection_roots`. The full-policy graph wrapper now returns validity for the actual constructed root sequence on the ensured heap, not just the base roots.

Fresh-disjointness is now packaged for the constructed collection-root sequence too. `GC.Gen.ChunkedReachabilityBridge.chunked_roots_disjoint_from_chunk_minor_pointers_above_zero` proves minor-pointer root sequences cannot point into a fresh chunk whose base is above `zero_addr`, and `GC.Gen.ChunkedRemembered.chunked_collection_roots_disjoint_from_chunk` applies it to the remembered-scan suffix. The full-policy graph wrapper now returns collection-root fresh-disjointness on the expansion branch, reducing the runtime-side freshness obligation to the true base-root sequence.

The expansion-safety obligation has been narrowed. `GC.Gen.CombinedGraph.chunked_major_objects_expansion_safe_from_values_miss_fresh` derives the old field-slot/header freshness half from `MH.chunk_disjoint_from_all` plus major-heap well-formedness, leaving runtime-facing callers to supply only that old major field values do not point into the future chunk. `GC.Gen.CheneyGraphReadiness.chunked_cheney_gc_correct_after_preflight_full_policy_and_post_reachable_image_from_base_roots_value_safety` exposes the same full-policy/post-reachable image theorem using this smaller value-safety premise.

The remaining preflight obligations now have named runtime-boundary predicates. `chunked_preflight_base_policy`, `chunked_preflight_expansion_value_policy`, and `chunked_minor_preflight_value_policy` package base-root validity, chunk-base policy, field-0 policy, base-root/fresh disjointness, fresh chunk acquisition facts, and old-field-value freshness into a single auditable surface. `chunked_cheney_gc_correct_after_preflight_full_policy_and_post_reachable_image_from_preflight_value_policy` is the current highest-level graph-readiness API for a multi-chunk caller following that policy.

The compact policy also has a fixed-heap compatibility bridge. `chunked_minor_preflight_value_policy_single_chunk_from_dense` derives the base chunked policy from the existing dense `roots_valid_nonblue` and `major_field_zero_no_minor` predicates, while preserving the same explicit expansion-branch fresh/value obligations. This keeps the current dense `minor_collect_full` contract aligned with the new chunked preflight surface.

The core chunked collection correctness bundle now has the same reduced expansion-safety surface. `GC.Gen.CheneyCorrectness.chunked_cheney_gc_correct_after_preflight_value_safety` derives `CG.chunked_all_major_object_expansion_safe` internally from old-field-value freshness plus chunk disjointness/collection shape, then reuses the existing bundled correctness theorem. `GC.SPOT.HeapExpansion` audits the wrapper, so both graph-readiness and core collection-correctness clients can now avoid carrying the slot/header half of expansion safety explicitly.

The compact preflight policy now exposes the exact branch premise needed by that core correctness wrapper. `GC.Gen.CheneyGraphReadiness.chunked_minor_preflight_value_policy_core_expansion_safety` proves `chunked_minor_preflight_value_policy` implies the expansion-branch facts expected by `chunked_cheney_gc_correct_after_preflight_value_safety`: fresh chunk disjointness, non-aliasing with the old free-list head, base/size facts, and old-field-value freshness. This gives runtime-facing callers one small audited bridge from the named policy to the core collection theorem.

Fixed-heap callers can reach that same core branch premise directly. `chunked_minor_preflight_value_policy_core_expansion_safety_single_chunk_from_dense` composes dense root/field-0 predicates with the single-chunk policy bridge, then exposes the core value-safety branch facts for `MH.single_chunk_major_heap major`. This keeps the current dense collection boundary aligned with both the graph-readiness theorem and the core collection-correctness theorem.

The fixed-heap graph-image wrapper also has a reduced value-safety surface now. `chunked_cheney_gc_correct_after_preflight_full_policy_and_post_reachable_image_single_chunk_from_dense_roots_value_safety` starts from dense root/field-0 predicates plus old-field-value freshness on the expansion branch, constructs the compact chunked preflight policy internally, and returns the post-reachable image isomorphism together with the full preflight policy facts on the ensured heap.

The collection-shape preflight invariant has the same reduction at the HeapInvariant layer. `chunked_collection_heap_shape_ensure_head_capacity_with_chain_blue_value_safety` derives the older all-object expansion-safety premise from chunk disjointness plus old-field-value freshness, then preserves collection shape, head capacity, free-list termination, and chain-blue through optional head expansion.

The budgeted multi-allocation preflight path now has the same value-safety reduction. `chunked_collection_heap_shape_ensure_head_capacity_alloc_list_with_budget_value_safety` supports arbitrary positive request lists bounded by a budget while requiring only chunk disjointness, fresh-chunk facts, and old-field-value freshness on the expansion branch.

That reduction is also specialized to minor-promotion demand. `chunked_collection_heap_shape_ensure_minor_promotion_budget_alloc_list_value_safety` and `chunked_collection_heap_shape_ensure_minor_promotion_head_capacity_allocs_value_safety` let Cheney-facing callers use the old-field-value freshness surface directly, including the canonical `minor_promotion_requests` list.

The split graph-readiness module now bridges the compact preflight value policy to Cheney's promotion and collection preflight surfaces. `chunked_cheney_promote_after_minor_promotion_head_preflight_from_preflight_value_policy` and `chunked_cheney_collect_after_minor_promotion_head_preflight_from_preflight_value_policy` recover the legacy expansion branch internally, so callers can use `chunked_minor_preflight_value_policy` instead of carrying `chunked_all_major_object_expansion_safe`.

Fixed-heap compatibility has the same reduced surface for those lower Cheney wrappers. The dense-root wrappers construct `chunked_minor_preflight_value_policy` from `roots_valid_nonblue`, `major_field_zero_no_minor`, and expansion-branch old-field-value freshness, then call the value-policy promotion/collection theorems over `MH.single_chunk_major_heap`.

For fixed-heap callers, old-field-value freshness is now derived from dense `well_formed_heap` plus fresh-chunk disjointness and fresh-base policy. This removes the raw old-field-value freshness quantifier from the dense-root promotion, collection, and post-reachable image wrappers while keeping the true multi-chunk value policy explicit.

The dense-to-single-chunk invariant bridge now covers the allocator-shape side conditions. `GC.Gen.SingleChunkInvariant` proves fixed-heap allocator search fuel positivity, dense object-header range facts for the single chunk, head-wosize compatibility, and a per-head/per-fuel dense-chain avoidance predicate that shrinks through free-list tails. Using that generalized predicate, it derives chunked `major_fl_valid`, `major_fl_above_zero`, `major_fl_blocks_fit`, `major_fl_chain_terminates`, and `chunked_chain_objects_blue` for `MH.single_chunk_major_heap g` from dense `major_heap_shape g fp`. `GC.SPOT.HeapExpansion` audits both the head-wosize and aggregate allocator-shape wrappers.

The dense-to-single-chunk collection-shape bridge is now complete for the current fixed-heap compatibility path. `GC.Gen.SingleChunkInvariant` derives chunked no-black, no-scan, no-pointer-to-blue, minor-to-major no-blue, and major-to-minor no-infix invariants from the existing dense predicates, then packages them with the allocator bridge into `chunked_collection_heap_shape_single_chunk_from_dense`. `GC.Gen.CheneyGraphReadiness` now uses this aggregate bridge in the fixed-heap promotion, collection, and post-reachable graph-image wrappers, so dense callers supply `collection_heap_shape minor major fp` plus the default allocator fuel equality instead of separately proving chunked collection shape, free-list termination, and chain-blue. The split module verifies in about 1.2s under `--z3refresh --split_queries always`, and `GC.SPOT.HeapExpansion` audits the new invariant and wrapper surfaces.

The current dense runtime-facing minor-collection preflight assumptions are now packaged as a named policy. `GC.Gen.CheneyGraphReadiness.fixed_heap_minor_collect_preflight_policy` bundles dense `collection_heap_shape`, dense root validity, dense field-0 no-minor policy, default allocator fuel, dense well-formedness, and the expansion-branch fresh-chunk facts. `fixed_heap_minor_collect_preflight_policy_from_dense_minor_collect_preconditions` derives that policy from the actual dense `minor_collect_full`-style proof facts by eliminating `collection_heap_shape` and specializing the allocator fuel to `SpecAlloc.alloc_search_fuel`. `fixed_heap_minor_collect_preflight_policy_core_expansion_safety` exposes the exact value-safety branch premise needed by the core chunked correctness wrapper. `chunked_cheney_collect_after_minor_promotion_head_preflight_single_chunk_from_dense_minor_collect_preconditions` now composes those facts directly into the chunked single-chunk collection preflight theorem at default fuel. `chunked_cheney_gc_correct_after_preflight_full_policy_and_post_reachable_image_single_chunk_from_dense_minor_collect_preconditions` does the same for the final post-reachable graph-image theorem. The reusable policy wrappers remain available for callers that want to factor those steps manually, and `GC.SPOT.HeapExpansion` audits all of these surfaces. The no-expansion specializations also show that when the current head already has enough minor-promotion capacity, the compact policy, core expansion-safety branch, chunked preflight result, and graph-image result follow without any fresh-chunk obligations. This is a small runtime-boundary alignment step: `minor_collect_full` still uses the dense collection implementation, but its existing preconditions now have a matching compact chunked-preflight policy surface.

The chunk-aware major-collector port has started in split helper modules. `GC.Spec.ChunkedSweepCoalesce.Defs` defines local chunked header/color helpers, chunked sweep over source chunk object lists, chunked zeroing, and a fused sweep/coalesce traversal that flushes each pending blue run at the end of every `objects_in_chunk` list before moving to the next chunk. This captures the key Stage 6 design constraint that coalescing is chunk-local and never merges across chunks. The module has an `.fsti` interface exposing only the traversal API and unfolding lemmas for downstream proofs, and `GC.SPOT.HeapExpansion` audits the public sweep/fused step lemmas at local rlimit 1. `GC.Spec.ChunkedSweepCoalesce.Compat` now proves fixed-heap single-chunk compatibility for `chunked_make_white`, `chunked_make_blue`, one-object `chunked_sweep_object`, list-level `chunked_sweep_aux`, full `chunked_sweep`, `chunked_zero_fields`, `chunked_flush_blue`, recursive `chunked_fused_aux`, and full `chunked_fused_sweep_coalesce`, with SPOT wrappers. `GC.Spec.ChunkedMark.Defs` now adds the first chunk-aware mark primitives: active-major field reads, active pointer classification, infix resolution guarded by active parent membership, gray/black color writes, child pushing, one-step marking, and a fuel-bounded mark loop, with public unfolding lemmas audited by SPOT. `GC.Spec.ChunkedMark.Compat` now bridges the primitive layer back to the dense fixed-heap model for field reads, active pointer classification, gray/black writes, no-scan checks, parent-closure address computation, valid-active infix resolution, empty/no-scan `chunked_mark_step` branches, and mark-aux empty/out-of-fuel base cases. `GC.Spec.ChunkedMark.NoPointerCompat` is a separate focused helper for the first scanning subcase: recursive `chunked_push_children` and the scanning `chunked_mark_step` branch agree with dense mark when the scanned suffix contains no pointer fields. `GC.Spec.ChunkedMark.PushCompat` generalizes that bridge with an explicit recursive readiness predicate for pointer fields, including valid-active infix parents, resolved-child header room, and branch-specific suffix readiness after gray writes; it proves full `chunked_push_children` and scanning `chunked_mark_step` fixed-heap compatibility under that predicate. `GC.Spec.ChunkedMark.MarkCompat` lifts the step bridge through `chunked_mark_aux` and top-level `chunked_mark` under a recursive mark-loop readiness predicate. `GC.Spec.ChunkedMarkBounded.Defs` starts the bounded-mark/rescan port with chunked bounded child pushing, bounded mark steps, inner drain loop, `MH.major_objects` rescan, and outer bounded mark loop; `GC.Spec.ChunkedMarkBounded.Compat` bridges chunked gray checks, bounded child-pushing, and one bounded mark step back to dense fixed-heap behavior under a bounded readiness predicate. `GC.Spec.ChunkedMarkBounded.LoopCompat` lifts bounded compatibility through non-black counting, `MH.major_objects` rescan, and the inner bounded drain loop using a separate object-list/header-room readiness predicate. `GC.Spec.ChunkedMarkBounded.OuterCompat` lifts that through the full outer bounded mark loop, proving `chunked_mark_bounded (single_chunk_major_heap g)` agrees with dense `MarkBounded.mark_bounded g` under recursive outer-loop readiness. `GC.Spec.ChunkedMajorGC.Defs` packages a first chunked major-GC spec shell: bounded mark followed by chunked fused sweep/coalesce, with single-chunk compatibility to dense `MarkBounded.mark_bounded` plus `SweepCoalesce.Defs.fused_sweep_coalesce`. `GC.Spec.ChunkedMajorGC.Correctness` starts the chunked correctness surface with `chunked_gc_postcondition` over `MH.major_objects` and chunked colors, plus a dense `gc_postcondition` to single-chunk bridge. Refreshed split-query profiles keep `ChunkedSweepCoalesce.Defs` under one second, `ChunkedSweepCoalesce.Compat` around 1.1 seconds, `ChunkedMark.Defs` under one second, `ChunkedMark.Compat` under one second, `ChunkedMark.NoPointerCompat` under one second, `ChunkedMark.PushCompat` around 9 seconds, `ChunkedMark.MarkCompat` around 3.8 seconds, `ChunkedMarkBounded.Defs` under one second, `ChunkedMarkBounded.Compat` under one second, `ChunkedMarkBounded.LoopCompat` around one second, `ChunkedMarkBounded.OuterCompat` around 2.4 seconds with `--z3refresh`, `ChunkedMajorGC.Defs` around one second, and `ChunkedMajorGC.Correctness` around 2.4 seconds; targeted helper checks, `cd mark-and-sweep && make verify-spec`, and `cd spot && make verify` pass.

Update 2026-06-13: `GC.Spec.ChunkedMajorGC.Correctness` now proves that the chunked bounded major-GC shell satisfies the chunked postcondition in the single-chunk compatibility case. The proof composes bounded-mark correctness, the dense fused sweep/coalesce correctness path, and the chunked single-chunk compatibility theorem; the one delicate step is an explicit no-gray bridge from `MarkBounded.mark_bounded_completes`/`SweepInv.no_gray_elim` to the forall shape expected by fused/coalesce. `GC.SPOT.HeapExpansion` audits the new theorem, targeted split-query verification passes, and both `cd mark-and-sweep && make verify-spec` and `cd spot && make verify` pass.

Update 2026-06-13: the single-chunk major-GC shell now also exposes the dense `full_gc_correctness` predicate for the dense heap corresponding to the chunked result. `chunked_major_gc_bounded_single_chunk_full_correctness` proves the chunked result equals `MH.single_chunk_major_heap h_final`, `h_final` satisfies dense full-GC correctness, and the chunked postcondition holds. This is still a compatibility bridge rather than true multi-chunk graph correctness, but it gives the next Stage 6 proof layer a small audited theorem tying the chunked shell back to all five dense correctness pillars.

Update 2026-06-13: the same single-chunk shell now exposes the dense graph corollaries directly. `chunked_major_gc_bounded_single_chunk_dense_graph_pillars` proves the corresponding dense final heap satisfies `major_gc_live_subgraph_isomorphism` and `major_gc_unreachable_final_blue`, while preserving the equality between the chunked result and the single-chunk dense view. This gives downstream chunked graph work a narrower bridge than the abstract `full_gc_correctness` predicate when it needs explicit live-edge, field, and unreachable-blue facts.

Update 2026-06-13: started the true chunked major-graph predicate layer in a new small module, `GC.Spec.ChunkedMajorGC.Graph`. It defines chunked major vertices over `MH.major_objects`, chunked field preservation, chunked field/edge predicates using active-major pointer classification, and a generic successor-preservation lemma from field preservation plus stable pointer classification. The module also provides single-chunk bridges for vertex membership and dense-to-chunked field preservation, with SPOT wrappers. The split module verifies in about one second with refreshed split queries.

Update 2026-06-13: connected the first true chunked graph predicate to the chunked major-GC shell. `GC.Spec.ChunkedMajorGC.Graph` now has a data-only `chunked_major_field_data_preserved` predicate for live-object payload preservation without requiring wosize equality, and `GC.Spec.ChunkedMajorGC.Correctness.chunked_major_gc_bounded_single_chunk_live_field_data_preserved` proves every dense-reachable live object satisfies it in the single-chunk chunked major-GC result. SPOT audits both the data bridge and shell theorem. Refreshed split-query profiles keep the changed chunked major graph/correctness modules around one second, and broad mark-and-sweep spec plus SPOT verification pass.

Update 2026-06-13: lifted that field-data bridge to full chunked field and successor preservation for live objects in the single-chunk major-GC result. `GC.Spec.ChunkedMajorGC.Graph.chunked_major_pointer_classification_preserved_single_chunk` records stable pointer classification for the compatibility range, `GC.Spec.ChunkedMajorGC.Correctness.chunked_major_gc_bounded_single_chunk_live_field_preserved` derives the missing live wosize equality from bounded-mark, sweep, and coalesce preservation lemmas, and `chunked_major_gc_bounded_single_chunk_live_successors_preserved` proves the chunked successor relation is preserved for every dense-reachable live object. SPOT audits the new contracts, refreshed profiles remain around one second, and broad mark-and-sweep spec plus SPOT verification pass.

Update 2026-06-13: exposed the edge-level live-source wrapper over chunked major successor preservation. `GC.Spec.ChunkedMajorGC.Graph.chunked_major_successors_preserved_elim` keeps the successor predicate abstract while giving clients the edge equivalence, and `GC.Spec.ChunkedMajorGC.Correctness.chunked_major_gc_bounded_single_chunk_live_edges_preserved` proves every dense-reachable source object's chunked major edges are equivalent before and after the single-chunk chunked major-GC shell. SPOT audits both wrappers; refreshed profiles remain around one second, and broad mark-and-sweep spec plus SPOT verification pass.

Update 2026-06-13: packaged the edge and vertex facts as the first chunked major live-subgraph preservation predicate. `GC.Spec.ChunkedMajorGC.Graph.chunked_major_live_subgraph_preserved` records live vertex survival plus live-source edge equivalence while keeping the predicate abstract behind intro/elim helpers, and `GC.Spec.ChunkedMajorGC.Correctness.chunked_major_gc_bounded_single_chunk_live_subgraph_preserved` proves it for the single-chunk chunked major-GC shell using dense reachability as the live set. SPOT audits the predicate helpers and the shell theorem; refreshed profiles remain around one second, and broad mark-and-sweep spec plus SPOT verification pass.

Update 2026-06-13: added the first generic multi-chunk live-subgraph bridge. `GC.Spec.ChunkedMajorGC.Graph.chunked_major_live_subgraph_preserved_from_fields` proves that live-object field preservation plus stable active-major pointer classification implies the abstract live-subgraph preservation predicate, with no single-chunk compatibility premise. SPOT audits the bridge; the graph module still profiles under one second, and broad mark-and-sweep spec plus SPOT verification pass.

Update 2026-06-13: split out a small major-heap read-framing helper for the next true chunked sweep/coalesce proofs. `GC.Spec.MajorHeap.ReadFrame.write_word_in_major_preserves_other_read` proves that a major-heap write preserves any disjoint successful word read, with tiny cons-hit/miss lookup helpers. Keeping this out of the already-large `GC.Spec.MajorHeap` module avoids slow rechecking; the helper profiles in about 0.6 seconds and is audited by SPOT.

Update 2026-06-13: added `GC.Spec.ChunkedSweepCoalesce.Preservation`, the first true chunked sweep/coalesce read-framing layer. It lifts the split `MajorHeap.ReadFrame` fact through `major_write_word_or_same`, chunked color writes, white/blue recoloring, one-object sweep steps, recursive sweep object-list traversal, zero-field writes, and flush-blue before/after/disjoint cases. The module exposes SPOT-audited contracts and profiles under one second with refreshed split queries; full mark-and-sweep spec verification and full SPOT verification pass.

Update 2026-06-13: extended the preservation split through fused sweep/coalesce recursion. `chunked_fused_aux_read_frame_ready` captures the exact disjointness obligations for pending blue-run flushes and black-object recoloring, and `chunked_fused_aux_preserves_other_read` proves the framed read survives the recursive fused traversal under that predicate. `Defs` now exposes a length-based empty fused-aux equation for clients. The preservation module still profiles under one second, with broad mark-and-sweep spec and SPOT closures passing.

Update 2026-06-13: added the first field-level bridge on top of fused traversal framing. `chunked_fused_aux_preserves_get_field_read_some` turns a framed successful payload-word read into equality of `chunked_get_field` before and after `chunked_fused_aux`, under the same readiness predicate and an explicit field-address witness. This is the bridge needed before packaging live-object field preservation; targeted profiling remains under one second and broad mark-and-sweep/SPOT closures pass.

Update 2026-06-13: started discharging fused-readiness side conditions. `chunked_fused_aux_read_frame_ready_from_all_after` proves the readiness predicate whenever the pending blue run is already after the protected read and every remaining object is also after that read. This isolates the suffix-after-live-object case needed for true chunked live-field preservation. The split preservation module still profiles at about one second, and broad mark-and-sweep/SPOT closures pass.

Update 2026-06-13: added the complementary prefix-through-live-object readiness layer. `chunked_fused_aux_live_read_frame_ready` follows `chunked_fused_aux` until the protected black target object, carrying exact pending-run/header disjointness obligations, then switches to the all-after suffix lemma. `chunked_fused_aux_preserves_get_field_from_live_target` packages that predicate into field equality across fused traversal. SPOT audits both contracts; the preservation module still profiles under one second and broad mark-and-sweep/SPOT closures pass.

Update 2026-06-13: added the first fused-sweep metadata framing helpers. `GC.Spec.ChunkedSweepCoalesce.Defs` now exposes `chunked_wosize_of_object_some/none`, while `GC.Spec.ChunkedSweepCoalesce.Preservation` proves same-slot read-back for `major_write_word_or_same` and that `chunked_set_object_color`, `chunked_make_white`, and `chunked_make_blue` preserve the protected object's wosize. SPOT audits the new contracts; targeted checks, broad mark-and-sweep spec verification, full SPOT verification, hygiene scans, and refreshed split-query profiling all passed.

Update 2026-06-13: discharged the main fused-traversal live-field readiness side condition from chunk-local object order. `GC.Spec.MajorHeap.objects_in_chunk_from_cons_step` exposes the cons/tail shape of a chunk-local object walk, and `GC.Spec.ChunkedSweepCoalesce.Preservation.chunked_fused_aux_live_read_frame_ready_from_chunk` derives the target-specific readiness predicate for a black live object field from `MH.objects_in_chunk c`, source/chunk wosize consistency, and the field witness. The proof is kept in the focused preservation split with small arithmetic/sequence helpers and a refreshed `--z3refresh --query_stats --split_queries always` profile at local rlimit 10. SPOT audits the new public wrapper.

Update 2026-06-13: combined the chunk-order readiness theorem with fused field-read framing into the first true chunked fused-sweep live-field data theorem. `GC.Spec.ChunkedMajorGC.Graph` now exposes small intro lemmas for abstract chunked vertices and field/data-preservation predicates, and `GC.Spec.ChunkedSweepCoalesce.Preservation.chunked_fused_aux_live_field_data_preserved_from_chunk` proves payload fields of a black live target are preserved by one chunk's fused traversal, assuming the target is known to remain a final vertex. This deliberately leaves live-vertex survival through coalescing as the next separate proof slice.

## Remaining steps

1. **Thread remaining side conditions through the final API.** The final graph theorem now has an append-based scanned-root construction wrapper, derives collection-root validity from base-root validity, derives collection-root fresh-disjointness from base-root fresh-disjointness plus fresh chunk base policy, derives old field-slot/header expansion safety from chunk disjointness and well-formedness, returns the full chunk-base/pointer-field/base-root-valid/constructed-root-valid/field-0 policy facts preserved by preflight expansion, has fixed-heap wrappers from dense root/field-0/collection-shape predicates, and the core collection-correctness bundle now also has a value-safety wrapper plus a compact-policy-to-core-branch bridge. The current dense runtime preconditions are packaged as `fixed_heap_minor_collect_preflight_policy`, with a no-expansion specialization for already-sufficient head capacity; the remaining API work is to supply the analogous initial multi-chunk chunk-base/base-root-valid/base-root-fresh-disjoint/old-field-value-fresh-disjoint/field-0 policy from the actual runtime-facing collection boundary.
2. **Port or bridge major GC/mark-sweep surfaces.** The runtime expansion policy assumes major GC can run before expansion. The chunked allocator/expansion path is far along, chunked sweep plus fused sweep/coalesce fixed-heap compatibility are in place, chunked mark now has readiness-based fixed-heap compatibility through top-level `chunked_mark`, bounded-mark compatibility now reaches top-level `chunked_mark_bounded`, and the single-chunk chunked major-GC shell now bridges to the chunked postcondition, dense `full_gc_correctness`, dense live-subgraph/unreachable-blue graph corollaries, live-object field/successor preservation, live-source edge preservation, and an abstract chunked live-subgraph preservation predicate. A generic multi-chunk field-preservation-to-live-subgraph bridge is available, and true chunked sweep/coalesce read-framing now covers color writes, self-wosize preservation across recoloring, sweep object-list traversal, zeroing, flush-blue cases, readiness-based fused traversal, readiness derivation from chunk-local live-object order, and one-chunk live-field data preservation under an explicit final-vertex premise. Next discharge live-vertex survival and wosize preservation for black targets through fused coalescing, then combine those with the field-data theorem and apply the generic live-subgraph bridge.
3. **Wire implementation/runtime acquisition.** Update the extracted/Pulse-facing allocation and collection entry points so they consume the chunked major heap representation, then update `alloc_gen.c` to allocate/register chunks, check alignment/disjointness/page-table facts, and call verified expansion/preflight functions.
4. **Extraction, docs, and integration tests.** Keep extraction stable, update design/runtime docs once the proof/API shape is final, and add integration scenarios where allocation or minor collection expands the major heap instead of failing.

## Strategy for acquiring new heap chunks

The verified code should not call `malloc` directly. The C runtime bridge acquires raw memory, page-rounds it, registers it with OCaml's page table, and checks the facts required by the verified model: base alignment, size, no overflow, virtual address bounds, disjointness from active major chunks, and disjointness from the minor range.

Once those facts hold, the verified expansion path treats the memory as a fresh `heap_chunk`: initialize it as one or more blue free-list blocks, prepend/link the fresh block chain to the global major free list, prove old reads/objects/graph facts are framed, and retry allocation or start Cheney promotion with enough head capacity. If promotion would otherwise fail because of OOM, the intended architecture is that preflight has already prevented that state by expanding before promotion begins.

## Audit checklist

Audit these parts to confirm the development is still on track:

- **Chunk model soundness:** `common/spec/GC.Spec.MajorHeap.*` for chunk disjointness, lookup uniqueness, word containment, object enumeration, add-chunk framing, and single-chunk compatibility.
- **Owned heap boundary:** `common/impl/GC.Impl.MajorHeap.fst` for `pts_to_range` ownership, indexed chunk resources, read/write accessors, and adapters to/from legacy `Heap.is_heap`.
- **Expansion and allocator contracts:** `mark-and-sweep/spec/GC.Spec.MajorAllocator*` for fresh chunk initialization, free-list validity/termination/fit, expand-on-OOM, head-capacity preflight, and split-allocation preservation.
- **Generational invariant lift:** `generational/spec/GC.Gen.HeapInvariant.*` for `chunked_collection_heap_shape`, preflight preservation, no-blue/no-infix/no-scan/no-pointer-to-blue, and `chunked_chain_objects_blue`.
- **Dense compatibility bridge:** `generational/spec/GC.Gen.SingleChunkInvariant.*` for fixed-heap-to-single-chunk allocator and collection-shape compatibility, including allocator/free-list shape, color invariants, no-scan/no-pointer-to-blue, and cross-generation field invariants.
- **Chunked Cheney/update correctness:** `generational/spec/GC.Gen.ChunkedPromote.*`, `GC.Gen.ChunkedCheney.*`, `GC.Gen.ChunkedUpdate.*`, and `GC.Gen.CheneyPreservation.*` for no-OOM/preflight, forwarding-target shape, allocator-shape preservation, old object/header/field preservation, and exact update effects.
- **Client theorem surface:** `generational/spec/GC.Gen.CheneyCorrectness.*` for the current chunked correctness bundle and edge/reachability corollaries. This is the best place to check whether the proof states the user-facing guarantees we ultimately need.
- **Graph bridge:** `generational/spec/GC.Gen.CombinedGraph.*` for chunked classification, edge introduction, old-view graph/reachability preservation, and the shape of future graph-morphism proofs.
- **SPOT audit:** `spot/GC.SPOT.HeapExpansion.fst` should wrap each important public contract without admits, so weak interfaces or missing premises show up in a smaller client module.
- **Runtime bridge assumptions:** later, audit `generational/ocaml-integration/verified_gc/alloc_gen.c` and extraction headers for the exact trusted facts passed from C to verified expansion.

## Validation bar

Before committing each proof milestone, keep checking:

- No new `admit()` or untracked `assume` in touched active files.
- Targeted F* verification of changed modules and interfaces.
- `cd spot && make verify` after public contract changes.
- Broad `make generational` after generational spec/impl changes.
- Warning 349 stays absent or is made explicit with stable split queries.
- Extraction and runtime tests once the implementation boundary changes.
